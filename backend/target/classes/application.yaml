spring:
  application: name=robot-warehouse-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/diploma_db
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true