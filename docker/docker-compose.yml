services:
  db:
    image: postgres:16
    container_name: warehouse_db_docker
    restart: unless-stopped
    environment:
      POSTGRES_DB: warehouse_db
      POSTGRES_USER: warehouse
      POSTGRES_PASSWORD: warehouse
    ports:
      - "5437:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata: